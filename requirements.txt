# PyTorch 1.9.0, compatible with pytorch-forecasting 0.9.1 and torchmetrics 0.6.0
torch==1.9.0

# PyTorch Lightning 1.3.8, compatible with pytorch-forecasting 0.9.1
pytorch-lightning==1.3.8

# Torchmetrics 0.4.1, compatible with PyTorch Lightning 1.3.8
torchmetrics==0.4.1

# PyTorch Forecasting 0.9.1
pytorch-forecasting==0.9.1

# Transformers 4.5.1, compatible with torch 1.9.0
transformers==4.5.1

# Pandas for data manipulation
pandas==1.3.0

# Requests library for making HTTP requests
requests==2.31.0

# Dotenv for loading environment variables
python-dotenv==1.0.0

# Numpy for numerical operations, compatible with torch 1.9.0
numpy==1.19.5

# Joblib for model serialization and parallel processing
joblib==1.3.2

# Scikit-learn for ML utilities
scikit-learn==0.24.2

# Flask for setting up the asynchronous server
flask[async]==2.1.0

# Gunicorn for running Flask apps in production with thread support
gunicorn[gthread]==20.1.0
